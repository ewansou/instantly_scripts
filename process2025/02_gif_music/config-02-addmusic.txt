# config-02-addmusic.txt

# The single video file you want to process:
INPUT_VIDEO="./02_gif_music/tempDisplay/input.mp4"

# Where to write the new video+music file:
OUTPUT_DIR="./02_gif_music/Output"

# Where to archive the original video after processing:
MOVED_DIR="./02_gif_music/Moved"

# The music track to overlay (include extension):
MUSIC_FILE="./02_gif_music/bg_music/background_music.mp3"


COMMAND='ffmpeg -i "$INPUT_VIDEO" \-i "$MUSIC_FILE" \-c:v copy -c:a aac \-map 0:v:0 -map 1:a:0 \"${OUTPUT_DIR}/with_music_$(basename "$INPUT_VIDEO")"'